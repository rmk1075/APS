T = int(input())
for t in range(T):
    M, N, x, y = map(int, input().split())
    count = 0

    t = x
    temp = []
    count = 0
    while t not in temp:
        t = (t + M) % N
        count +=

    if y in temp:
        print
